version: 2

project_name: kache

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: kache
    binary: kache
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.Version={{ .Version }}

archives:
  - id: kache
    builds:
      - kache
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

dockers:
  - image_templates:
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-amd64"
    use: buildx
    dockerfile: Dockerfile.goreleaser
    ids:
      - kache
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.title={{ .ProjectName }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"
      - "--label=org.opencontainers.image.version={{ .Version }}"
  - image_templates:
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-arm64"
    use: buildx
    dockerfile: Dockerfile.goreleaser
    ids:
      - kache
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.created={{ .Date }}"
      - "--label=org.opencontainers.image.title={{ .ProjectName }}"
      - "--label=org.opencontainers.image.revision={{ .FullCommit }}"
      - "--label=org.opencontainers.image.version={{ .Version }}"

docker_manifests:
  - name_template: "ghcr.io/hoangnguyendev3/kache:{{ .Version }}"
    image_templates:
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-amd64"
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-arm64"
  - name_template: "ghcr.io/hoangnguyendev3/kache:latest"
    image_templates:
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-amd64"
      - "ghcr.io/hoangnguyendev3/kache:{{ .Version }}-arm64"

release:
  github:
    owner: hoangNguyenDev3
    name: kache
  draft: false
  prerelease: auto
  name_template: "v{{ .Version }}"
